# Instalacao

# Windows

Requisitos para impressoras USB:
- Zadig

Download: https://zadig.akeo.ie/

# Linux

Requisitos para impressoras USB:
- Cups

Intalacao do Cups:

sudo apt-get update
sudo apt-get install cups
sudo systemctl enable cups
sudo systemctl start cups

Dando permissao owner para o cups

sudo usermod -a -G lpadmin [ digite seu usuario sem os cochetes ]